import {NextResponse} from 'next/server';import {validSession} from '../../../../lib/auth';import {sb,tables} from '../../../../lib/supabase';
export async function GET(request){if(!validSession(request))return NextResponse.json({error:'Non autorisé'},{status:401});const q=request.nextUrl.searchParams.get('q')||'';try{const filter=q?`&or=(id.eq.${encodeURIComponent(q)},code.eq.${encodeURIComponent(q)},email.ilike.*${encodeURIComponent(q)}*,name.ilike.*${encodeURIComponent(q)}*)`:'';const rows=await sb(`${tables.members}?select=*&order=updated_at.desc&limit=30${filter}`);return NextResponse.json({members:rows});}catch(e){return NextResponse.json({error:e.message},{status:500})}}
